const BASE_URL = `https://ark-backend.herokuapp.com`;
const user = "user";
const userToken = "userToken";
const allProducts = "allProducts";
const cart = "cart";
const favs = "favs";
const home = "home";

export { BASE_URL, user, userToken, allProducts, cart, favs, home };
